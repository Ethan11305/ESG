
## 問題
- 
- 目標：透過 API（LLM）自動讀取「ESG報告書.pdf」中的文字內容，並將敘述依據 ESG 三大面向分類為 E（環境）、S（社會）、G（治理）。
- 輸出格式：以 表格方式輸出
- 需求細節：
  1. PDF 先被轉成可供分析的純文字（必要時以「每頁」或「章節」切塊）。
  2. 送入 LLM API 進行分類與摘要（避免逐字搬運，偏向「可引用的重點句/段落」）。
  3. 每條重點建議保留「來源頁碼/段落標記」以便回查。
## 解法
- 

## 例子 / Code
```python
import os
from langchain_openai import ChatOpenAI
from langchain_core.prompts import PromptTemplate

# 1. 設定 API Key（請替換為你的金鑰）
os.environ["OPENAI_API_KEY"] = OPENAI_API_KEY

# 2. 定義模型（使用 gpt-4o 進行深度邏輯分析）
llm = ChatOpenAI(
    model="gpt-4o",
    temperature=0
)

# 3. 建立精確的偵測指令
# 根據廣達 2024 年永續報告書特性，檢查數據重編與 2050 淨零承諾
template = """
你是一位資深的永續發展（ESG）稽核專家。
請針對以下「廣達電腦」2024 年永續報告書內容，
運用「綠色洗白七宗罪」框架進行偵測。

【分析要求】
1. 鎖定電子製造業高風險領域：
   - 碳中和路徑（E）
   - 綠電比例（E）
   - 供應鏈人權（S）
   - 董事會多元化（G）
2. 針對 E、S、G 各領域找出至少 4 個核心主張。
3. 輸出一個 Markdown 表格。

【偵測指標：綠色洗白七宗罪】
- 隱瞞代價
- 空口說白話
- 含糊不清
- 崇拜假標章
- 無中生有
- 兩害相權取其輕
- 虛假陳述

【報告內容摘要】
{context}

請生成報告表格：
"""

prompt = PromptTemplate.from_template(template)
chain = prompt | llm

# 4. 執行分析
# 注意：選取 15–45 頁，通常是氣候策略與社會績效最密集的區段
target_context = "\n".join(
    [p.page_content for p in pages[15:45]]
)

print("⌛ 正在分析廣達 2024 永續報告書，請稍候...")

response = chain.invoke(
    {"context": target_context}
)

print("\n### 【步驟一】廣達電腦 2024 內部聲明偵測與初步評級報告")
print(response.content)
```
